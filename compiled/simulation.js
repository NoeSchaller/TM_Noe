"use strict";
class Simulation {
    constructor(width, height, id, mapLoad, mapCreate, zoom = 0.8, mouse = true, debug = false, background = 0xccac0) {
        /*
        this.game = new Phaser.Game({
            width: width,
            height: height,
            backgroundColor: background,
            type: Phaser.WEBGL,
            canvas: document.getElementById(id),
            scene: [
              new Simul(
                this.robots,
                this.walls,
                this.marks,
                this.zones,
                mapLoad,
                mapCreate,
                mouse
              ),
              new Over(height, zoom),
            ],
            physics: {
              default: "matter",
              matter: {
                gravity: { y: 0, x: 0 },
                debug: debug,
              },
            },
            plugins: {
              scene: [
                {
                  key: "PhaserRaycaster",
                  plugin: PhaserRaycaster,
                  mapping: "raycasterPlugin",
                },
              ],
            },
          });
          */
    }
}
